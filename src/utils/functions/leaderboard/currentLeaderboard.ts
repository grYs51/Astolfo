import { voice_stats } from "@prisma/client";
import { getVoiceStatsType } from "./leaderboard";

export const getCurrentVoiceStats: getVoiceStatsType = async () => {
return [] as voice_stats[]
}
